{
  "name": "webapp",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "client": "yarn workspace client start",
    "build": "yarn workspace client build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.1.6"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "@types/node": "^20.5.1",
    "prettier": "^3.0.2"
  }
}
